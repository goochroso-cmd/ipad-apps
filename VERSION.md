# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v21 本番** | `ipad-apps-cache-v15` |
| まるち君 | 旧方式のまま | — |

## v21 について

**中身は v13 と同じ。** 測定用・検証用の表示はすべて外した。
音量つまみもフェードもクロスフェードも、これまで通り動く。

v14（登録が固まる）は取り消し済み。v15〜v20 は原因究明のための測定版・検証版で、本番用ではない。

## 2026-08-11 に実測で確定したこと

| 疑い | 結果 |
|---|---|
| iPadが読み込みを捨てている | **シロ**（曲は丸ごと手元にあり rs=4） |
| AudioContext が眠る/中断される | **シロ**（ずっと running） |
| フェードの立ち上がり | **シロ**（音量は 1.00 固定だった） |
| **Web Audio 接続（createMediaElementSource）** | **★クロ** |

**「息継ぎ」の正体＝再生位置が0.6〜0.7秒後に約0.17秒ぶん巻き戻ること。**
アプリは currentTime を一度も書いていない。**Safari 自身が巻き戻している。**
Web Audio 接続を切ると**巻き戻りは完全に消えた**。

### ただし、切ると音量が使えない

iPad の Safari は `<audio>` の `volume` を**無視する**（v20で実測）。
`el.volume = 0.3` は設定値としては入るが、**音の大きさは変わらない**。

| | 音量・フェード | 息継ぎ |
|---|---|---|
| Web Audio を通す（現行） | 使える | 出る |
| 通さない | 使えない | 出ない |

**この二択が、いま解くべき本当の問題。**
