# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v25 本番** | `ipad-apps-cache-v19` |
| まるち君 | 旧方式のまま | — |

**中身は v13 と同じ。** 測定用・実験用の表示はすべて外してある。
音量つまみ・フェード・クロスフェードは通常どおり動く。

## 2026-08-11 に実測で確定したこと

### 症状の正体

**再生位置が、鳴り出しの0.5〜1.1秒後に約0.18秒ぶん巻き戻る。** これが「息継ぎ」。
アプリは `currentTime` を書いていない。**Safari 自身の挙動。**

### 原因

**`<audio>` を Web Audio に通していること**（`createMediaElementSource`）。
切ると巻き戻りは完全に消える。

### 潰した案（すべて実機で確認）

| 案 | 結果 |
|---|---|
| 読み込みを温める（v14 修正4） | 的外れ。しかも登録が固まる事故 |
| 系統B（1曲＝専用の箱）へ寄せる | 同じ症状。しかも修正1が無いぶん後退 |
| Web Audio を切る | 巻き戻りは消えるが**音量が一切効かなくなる** |
| 毎回まっさらな `<audio>` を作る | **巻き戻る**（1100ms）。要素の使い回しは無関係 |
| 消音で先走り→頭出し→音量up | **頭出しでまた巻き戻る**（500ms）。破棄 |

### 確定したルール

**Web Audio に繋いだ状態では、再生開始または頭出しのたびに、必ず1回 約0.18秒の巻き戻りが起きる。**

### 次の候補（未検証・複雑）

**曲の先頭だけを音の生データに変換して手元に持ち、押した瞬間はそれを鳴らす。**
`<audio>` の方は裏で先に走らせておき、巻き戻りが済んだ頃に鳴り替える。

- 先頭3秒ぶんの生データは1曲あたり約1MB。12曲でも12MB程度で収まる見込み
- 生データ側は Web Audio で完全に制御できるので、音量もフェードも効く
- **鳴り替えの継ぎ目をどう揃えるかが難所。必ず実測してから作ること**
