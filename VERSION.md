# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v27 本番** | `ipad-apps-cache-v21` |
| まるち君 | 旧方式のまま | — |

**中身は v13 と同じ。** 測定用・実験用の表示はすべて外してある。
音量つまみ・フェード・クロスフェードは通常どおり動く。

## 症状の正体（実測で確定）

**再生を始めると、約500ミリ秒後に再生位置が 0.27秒ぶん巻き戻る。1回だけ。**
アプリは `currentTime` を書いていない。**Safari 自身の挙動。**

**原因は `<audio>` を Web Audio に通していること**（`createMediaElementSource`）。
切ると巻き戻りは完全に消える。ただし iPad Safari は `<audio>` の音量を無視するので、
切ると音量つまみもフェードも死ぬ。

## 潰した案（すべて実機で確認済み）

| 案 | 結果 |
|---|---|
| 読み込みを温める（v14 修正4） | 的外れ。登録が固まる事故つき |
| 系統B（1曲＝専用の箱）へ寄せる | 同じ症状。しかも後退 |
| Web Audio を切る | 巻き戻りは消えるが音量が完全に死ぬ |
| 毎回まっさらな `<audio>` を作る | 巻き戻る |
| 消音で先走り→頭出し→音量up | 頭出しでまた巻き戻る |
| `src` を入れ直してから鳴らす | **A/B比較で同じ。-0.272秒/約500ミリ秒。無関係** |

**「Web Audio に繋いでいる限り、鳴らすたびに必ず1回起きる」が確定。**

## 残る候補（未検証）

曲の先頭数秒だけを「音の生データ」として持ち、押した瞬間はそれを鳴らす。
`<audio>` は裏で先に走らせておき、巻き戻りが済んだ頃に鳴り替える。
生データ側は要素を経由しないので巻き戻りが起きず、音量もフェードも効く。
先頭3秒なら1曲約1MB、12曲でも12MB程度。**継ぎ目の同期が難所。必ず測ってから作ること。**
