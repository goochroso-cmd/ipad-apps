# 現在の版

最終更新: 2026-08-10

| アプリ | 版 | md5 | sw.js |
|---|---|---|---|
| さいせい君 (`さいせい君_standalone_改良版.html`) | **2026-08-10 v13** | `5ffff04c` | `ipad-apps-cache-v8` |
| まるち君 (`まるち君_standalone_改良版.html`) | 旧方式のまま（作り替え未着手） | — | — |

## この版について

**v14 を取り消して v13 に戻した。**

v14 で入れた「停止後の読み込み温め」（修正4）が、
**パッドに曲を登録すると「準備中…」のまま固まる**という新しい不具合を起こしたため。

- つまづき（再生→停止→再生の息継ぎ）は v13 のまま残っている。未解決。
- `sw.js` の番号は v6 → **v8**（iPad が古い版を掴み続けないように、番号は必ず上げる）

## 版の履歴

| コミット | md5 | 内容 |
|---|---|---|
| `b5a13c8` | `62cc6876` | v14 停止後の読み込み温め → **登録が固まるため取り消し** |
| `63dc823` | `5ffff04c` | **v13（現在これに戻している）** 停止後の頭出し修正＋版数表示 |
| `835b11e` | — | 系統B `saisei-cuezy.html` 追加（実機未検証） |
| `0d105a4` | `6ae836af` | v12 起動時のタップ案内 |
| `73bb0cf` | `d95bc646` | 診断表示 |
| `422e5aa` | `8bf5d3af` | 修正1＋修正2-1 |
| `0622b9e` | `4ad4bd61` | 改良版v3（`<audio>` 方式へ全面変更） |

## 版の確認のしかた

アプリ画面の**左上**に、黄色い枠で版数が出る。
ここが `2026-08-10 v13` になっていれば、この版が入っている。
