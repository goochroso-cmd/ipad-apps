# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v26 実験版★AB比べ** | `ipad-apps-cache-v20` |

## ★本番用ではない（パッドの動作は v25 のまま）

## 分かってきたこと

数字の上では**1回目も2回目も同じように巻き戻っている**（約0.18秒）。
しかし親分の耳では**1回目と2回目は明らかに違う**。
→ 100ミリ秒ごとの測定では見えていない差がある。

### 1回目と2回目の構造上のただ一つの違い

| | 直前に何が起きたか |
|---|---|
| 1回目 | `src`（曲のファイル）を**入れ直して読み込んだ直後** |
| 2回目 | 一時停止からの**再開**。`src` はそのまま |

起動時、16個の箱は無音ファイルで解禁される。1回目の前に箱の中身が曲のファイルへ差し替わる。
2回目にはそれが起きない。

## v26 で試すこと

同じ箱を使い回して、次の2つを聴きくらべる。

| ボタン | 内容 |
|---|---|
| **A ふつうに鳴らす** | 頭出しして `play()`（＝ふだんの2回目） |
| **B 入れ直して鳴らす** | `src` を入れ直し `load()` してから `play()`（＝1回目と同じ条件） |

測り方を画面の更新ごと（約16ミリ秒）に細かくして、巻き戻りを全部拾う。

### 判定

- **Bのほうがきれいに聞こえる** → **答え。停止したら箱の中身を入れ直しておけばよい**
- **AもBも同じ** → この線も消える。別を探す

A と B を交互に何度か押して聴きくらべること。
