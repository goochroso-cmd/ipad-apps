# 現在の版

最終更新: 2026-08-11（夜）

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v31** | `ipad-apps-cache-v26` |
| まるち君 | 旧方式のまま | — |

| ファイル | md5 |
|---|---|
| `さいせい君_standalone_改良版.html` | `848d45fb71cec9fa570f26a871becacb` |
| `sw.js` | `223623ccda8b5a1360cd87979e28c8e6` |

QR: **`QR_v31.png`**（`?v=31`）

---

# ★★ 今日いちばん大きい成果 ─ 再現手順が確定した

登録トラブルは3回直して3回とも再発した。**理由は、確かめる手順が無かったから。**
2026-08-11 に手順が確定した。

> **設定 → Safari → 「履歴とWebサイトデータを消去」**
> **→ QRでアプリを開く → いきなり12曲を続けて登録**
>
> → **4曲目あたりから「準備中…」のまま操作できなくなる。再起動すると直る。**

**ホーム画面のアイコンを消すだけでは再現しない。** Safari の保管庫が残るため。
**これからは「直った」を、この手順で確かめてから書くこと。**

---

# v31 で直したこと（修正C）

## 症状

曲を登録すると、そのパッドが「準備中…」のまま押しても反応しない。再起動するまで戻らない。

## v30 に残っていた穴（コードを読んで確認・推測ではない）

`armPad()` には「**同じパッドの準備が進行中なら、その進行中の仕事をそのまま返す**」
という重複防止がある（`_armingPromises`）。

そのため:

1. 登録が始めた準備が詰まっている
2. その間にパッドを押す
3. 押した側は**詰まった仕事に合流するだけ**
4. **v30 の修正A（`load()` のやり直し）は一度も走らない**

→ **修正Aは、いちばん必要な場面でだけ素通りされていた。**
修正B（25秒→6秒）は合流の時間を縮めただけで、合流そのものは残っていた。

さらに、12曲を続けて登録すると **12個の `<audio>` が同時に読み込みを始める**。

## 直した内容（1箇所だけ）

**`importFileToPad` から `armPad()` の呼び出しを完全に削除した。**

- 登録は **保存するだけ**
- 読み込みは **押されたときだけ**（`triggerPlay` → `armPad`）

押した瞬間は iOS が確実に「人が触った」と認める瞬間。
進行中の仕事も無いので、**失敗しても押し直せば必ずやり直せる。**

**コードを足すのではなく削る修正。** 親分の「複雑な処理よりシンプルな方がいいのでは？」に沿う。

## 代償（承知のうえ）

- 登録した直後のパッドは「準備中…」のままになる
- そのパッドの**初回の押下だけ一拍待つ**
- 次に起動したときは `armRange()` がまとめて用意するので、本番の使用には影響しない

---

# 直しの履歴（この症状は4回目）

| 回 | 版 | やったこと | 結果 |
|---|---|---|---|
| 1 | `8bf5d3af` | 修正2-1: 登録時の15秒待ちを削除 | また出た |
| 2 | v28 `dc155090` | 修正2-2: 保存より先に読み込む | また出た |
| 3 | v30 `d11980f5` | 修正A（load やり直し）＋修正B（25→6秒） | また出た（再現手順で確認） |
| 4 | **v31** `848d45fb` | **修正C: 登録時の armPad を廃止** | **← 実機未確認。いまここ** |

**v30 の VERSION.md に「A でも再発するなら、登録時に読み込もうとするのを完全にやめること」
と書いてあった。v31 はそれを実行したもの。**

## v31 でも再発したときの次の一手

`<audio>` の枠（`PRIMARY_SLOTS = 12` ＋ 予備4 ＝ 16個）の取り合いを疑う。
起動時の `unlockSlots()` が16個を1回ずつ鳴らして解禁しているところと、
`_assignSlot()` の枠の取り上げ処理を見る。

---

# 息継ぎ（0.27秒の巻き戻り）── 未解決・今は触らない

再生開始の約0.5秒後に、再生位置が 0.27秒ぶん巻き戻る。1回だけ。
アプリは `currentTime` を書いていない。**Safari 自身の挙動。**

原因は `<audio>` を Web Audio に通していること（`createMediaElementSource`）。
切ると消えるが、iPad Safari は `<audio>` の音量を無視するため、音量もフェードも死ぬ。

## 潰した案（すべて実機で確認済み。二度と提案しない）

| 案 | 結果 |
|---|---|
| 読み込みを温める | 的外れ |
| 系統B へ寄せる | 同じ症状。後退 |
| Web Audio を切る | 巻き戻りは消えるが音量が死ぬ |
| 毎回まっさらな `<audio>` を作る | 巻き戻る |
| 消音で先走り → 頭出し → 音量up | 頭出しでまた巻き戻る |
| `src` を入れ直してから鳴らす | A/B比較で同じ。無関係 |
| WAV形式で鳴らす | 同じくつまづく。ファイル形式は無関係 |
| **`sampleRate: 44100`（案2-a／v29）** | **効かない。白** |

## 残っている案

**案2-b（`latencyHint` を変える）のみ。**
それでも駄目なら WebCodecs。ただし作り直しに近いので最後の手段。

**登録トラブルが決着するまで、息継ぎには手を出さない。**
2つ同時に触ると切り分けができなくなる（2026-08-11 にそれで失敗した）。

---

# 実測でわかっている合格点（v30 で確認済み）

- **120分・290MB の音声ファイルを登録して再生できた**
- 旧方式（メモリに波形を展開する方式）では**物理的に不可能**だったサイズ
- **`<audio>` 方式への全面変更は正解だったと、ここで証明された**
