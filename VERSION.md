# 現在の版

最終更新: 2026-08-11（夜）

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v32** | `ipad-apps-cache-v27` |
| まるち君 | 旧方式のまま | — |

| ファイル | md5 |
|---|---|
| `さいせい君_standalone_改良版.html` | `7991fbd1a5648af852aa9994c709aea4` |
| `sw.js` | `c57460f17e92890cc2fed07f78555f1c` |

QR: **`QR_v32.png`**（`?v=32`）
土台: **v30（`7fbb4ef`）** に 修正E を足したもの。**v31 は却下。**

---

# v32 で直したこと（修正E）── 「諦めない」

## 何が起きていたか（実測でここまで絞れた）

**準備中のパッドをダブルタップすると「〇〇 を準備しています…」が出る。**
**しかしそれが消えて、パッドは「準備中…」のまま鳴らない。**

つまり:

- ダブルタップは**届いている**
- 読み込みも**始めようとしている**
- `armPad()` が **「失敗」を返している**

失敗の入口はほぼ1つ ── **v30 でくろが 25秒 → 6秒 に縮めた時間切れ（修正B）。**

## これで全部の観察が説明できる

| 観察 | 説明 |
|---|---|
| 初回起動でだけ出る | 初回は iPad がアプリ本体の保管庫づくりで忙しい → 6秒に間に合わない |
| v30 は4曲目以降で固まった | 最初の数曲は間に合った。12本同時で遅くなり脱落した |
| v31 は1曲目から固まった | 登録時に温めないので、押した瞬間から6秒しかない |
| 再起動すると直る | 2回目は保管庫ができていて空いている → 間に合う |
| ホーム画面から消すだけでは再現しない | Safari の保管庫が残るため |

**★ 6秒に縮めたのは v30 でくろが入れた修正B。間に合っていたものまで切り捨てていた。**

## 直した内容（修正E・4箇所だが1つの考え）

**「待つのをやめる」ことと「諦める」ことは別。`<audio>` は待つのをやめても読み込みを続けている。**

| 番号 | 内容 |
|---|---|
| E-1 | `armPad`: すでに「あとから待ち」が動いているなら `load()` をやり直さない（読めた分を捨てない） |
| E-2 | `armPad` の失敗時に、諦めず `_watchLateReady()` に切り替える |
| E-3 | `_watchLateReady()` を新設。時間切れのあとも見張り、準備できた瞬間に `armed = true` にする |
| E-4 | 画面側 `engine.onLateReady`: 「準備中…」を外し、「〇〇 の準備ができました。」と出す |

## ★ 修正E は「直し」であると同時に「測定」

| 実機で起きること | 意味 |
|---|---|
| 準備中が**しばらくして勝手に外れる** | **原因は「遅いだけ」で確定。**次は D（初回の下ごしらえを先に済ませる）で速くする |
| 準備中が**いつまでも外れない** | **読み込みが一度も始まっていない。**まったく別の犯人。D をやっても無駄と分かる |

**どちらに転んでも、次に何をすべきかが確定する。**

---

# ★ 次の一手（親分の案・修正D）

**「初回起動のときに、下ごしらえを全部先に済ませてから、曲の登録を受け付ける」**

> 使用者的には、曲を登録していないけど「2回目の起動状態」になっていればよい（親分）

1. アプリ本体の保管庫（Service Worker）ができるまで待つ
2. 曲を入れる保管庫（データベース）を開いて空回しし、初期化を済ませる
3. 音を鳴らす装置16個の解禁を済ませる
4. 全部終わってから「使えます」と出す

代償は**初回起動が数秒遅くなる**ことだけ。2回目以降は変わらない。
**修正E で「遅いだけ」と確定したら、これが本命の仕上げになる。**

---

# 直しの履歴（この症状は5回目）

| 回 | 版 | やったこと | 結果 |
|---|---|---|---|
| 1 | `8bf5d3af` | 修正2-1: 登録時の15秒待ちを削除 | また出た |
| 2 | v28 `dc155090` | 修正2-2: 保存より先に読み込む | また出た |
| 3 | v30 `d11980f5` | 修正A（load やり直し）＋**修正B（25→6秒）** | また出た（4曲目以降） |
| 4 | v31 `848d45fb` | 修正C: 登録時の armPad を廃止 | **悪化（1曲目から）。却下** |
| 5 | **v32** `7991fbd1` | **修正E: 時間切れでも諦めない** | **← 実機未確認。いまここ** |

**1〜4 はすべて「登録のしかた」を直していた。全部同じ層。**
**5 で初めて「押したあとの待ち方」に手を入れた。**

---

# 息継ぎ（0.27秒の巻き戻り）── 未解決・今は触らない

**案2-a（`sampleRate: 44100`）は白**（v29 実機確認）。
**残る案は 2-b（`latencyHint` を変える）だけ。** それでも駄目なら WebCodecs（最後の手段）。

**登録トラブルが決着するまで、息継ぎには手を出さない。**

---

# 実測でわかっている合格点（v30 で確認済み）

- **120分・290MB の音声ファイルを登録して再生できた**
- 旧方式（メモリに波形を展開する方式）では**物理的に不可能**だったサイズ
- **`<audio>` 方式への全面変更は正解だったと、ここで証明された**
