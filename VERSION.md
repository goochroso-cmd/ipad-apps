# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v16 測定版2** | `ipad-apps-cache-v10` |
| まるち君 | 旧方式のまま | — |

## この版について

**v13 に「測るだけ」の表示を足したもの。再生の処理は1行も変えていない。**

### v15 で分かったこと

2回目の再生でも `手元のデータ=1本 0.0〜151.7 / rs=4` ＝ **曲は丸ごと手元にあった。**
**読み込みは原因ではない。** v13の修正3・v14の修正4は、存在しない問題を直そうとしていた。

### v16 で測ること

**音の装置（AudioContext）の状態**を追加表示する。
playPad は `suspended`（眠っている）しか見ておらず、
iOS 固有の `interrupted`（中断されている）を見逃している疑いがある。

- `音の装置=動いている` → 装置は正常。原因はさらに別
- `音の装置=★中断されている` / `★眠っている` → **これが犯人**
