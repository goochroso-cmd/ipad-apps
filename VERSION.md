# 現在の版

最終更新: 2026-08-11

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-11 v30** | `ipad-apps-cache-v25` |
| まるち君 | 旧方式のまま | — |

| ファイル | md5 |
|---|---|
| `さいせい君_standalone_改良版.html` | `d11980f52c282cf61c485f4b72818f2e` |
| `sw.js` | `d32fca8f1613bc9d7609798ba3964f37` |

---

## v30 で直したこと（登録トラブル・3回目の挑戦）

**★ この症状への修正は、これで3回目。過去2回はどちらも再発した。**

| 回 | 版 | やったこと | その後 |
|---|---|---|---|
| 1 | `8bf5d3af` | 修正2-1: 登録時の15秒待ちを削除 | また出た |
| 2 | v28 `dc155090` | 修正2-2: 保存より先に読み込む | **また出た** |
| 3 | **v30** | 下記A＋B | ← いまここ |

### 直した内容（2箇所）

**A. `armPad`: まだ鳴らせる状態でないなら、同じ曲でも `load()` をやり直す**

旧コードは「`src` が同じ」を理由に `load()` をやり直さなかった。
そのため一度読み込みに失敗すると、**押しても押しても復帰せず、再起動するまで固まった。**
この穴は v28 の説明文に原因として書いてありながら、**v28 では直していなかった。**

**B. `_waitReady`: 失敗確定までの待ちを 25秒 → 6秒**

25秒だと、その間に押しても進行中の待ちに合流するだけで無反応に見える。
Aだけでは押し直しがすぐ効かないため、2つで1組。

### なぜ3回も再発したか（くろの見立て）

3回とも「**登録したその瞬間に読み込ませる**」という同じ発想だった。
だが iOS が「人が触った」と認めるのは**パッドを押す行為**であって、
**ファイル選択の画面から戻ってきた瞬間ではない**。ここは iOS 任せで、こちらから確実にできない。
→ 同じコードでも**タイミング次第で出たり出なかったりする。**

**A は「押されたときに読み込む」へ発想を寄せる第一歩。**
**A でも再発するなら、登録時に読み込もうとするのを完全にやめること。**

---

## v29（息継ぎ 案2-a）は取り下げ済み

`sampleRate: 44100` を入れた版。息継ぎは消えず、登録トラブルも出たため撤回。
ただし **後の調査で、登録トラブルは v29 のせいではなかった**（v28 に戻しても再発）。
**44100 が息継ぎに効かないことだけは確定。案2-a は白。次は案2-b（latencyHint）。**

---|---|---|
| さいせい君 | **2026-08-11 v28 修正版** | `ipad-apps-cache-v22` |
| まるち君 | 旧方式のまま | — |

| ファイル | md5 |
|---|---|
| `さいせい君_standalone_改良版.html` | `dc15509070a55bdec921df097cac7a76` |
| `sw.js` | `08eba5a4f947d8af5edf00efbef7d542` |

---

## v28 で直したこと（修正2-2）

**症状**: 曲を登録すると、そのパッドが「準備中…」のまま再起動するまで押せない。

**原因**: iOS は「人が触った、まさにその瞬間」にしか `<audio>` の読み込みを始めない。
登録処理が先にデータベースへの保存を待っていたため、そこで権利が切れていた。
そのあと読み込もうとしても始まらず、25秒待って失敗 → 「準備中…」で固定。
さらに失敗しても `el.src` が残るので、押し直しても `load()` が呼ばれず永久に復帰しない。

**修正（2箇所）**

1. `importFileToPad` の順番を入れ替え、**保存より先に `armPad()` を呼ぶ**（await しない）
2. `savePad` が **新しい物体を作り直すのをやめ**、`Object.assign` でその場で書き換える

2 は別件12（設定画面を触ったパッドが操作不能）の原因でもあるため、一緒に直る見込み。

**v27 と v13 の差は版数の文字1行だけ**（実測）。退行ではなく、v13から残っていたバグ。

---

## 息継ぎ（0.27秒の巻き戻り）── 未解決

再生開始の約0.5秒後に、再生位置が 0.27秒ぶん巻き戻る。1回だけ。
アプリは `currentTime` を書いていない。**Safari 自身の挙動。**

原因は `<audio>` を Web Audio に通していること（`createMediaElementSource`）。
切ると消えるが、iPad Safari は `<audio>` の音量を無視するため、音量もフェードも死ぬ。

### 潰した案（すべて実機で確認済み）

| 案 | 結果 |
|---|---|
| 読み込みを温める | 的外れ |
| 系統B へ寄せる | 同じ症状。後退 |
| Web Audio を切る | 巻き戻りは消えるが音量が死ぬ |
| 毎回まっさらな `<audio>` を作る | 巻き戻る |
| 消音で先走り → 頭出し → 音量up | 頭出しでまた巻き戻る |
| `src` を入れ直してから鳴らす | A/B比較で同じ。無関係 |
| **WAV形式で鳴らす** | **同じくつまづく。ファイル形式は無関係** |

### 次の候補

`AudioContext` を `sampleRate: 44100` で作る ＋ `latencyHint` を変える。
装置は 48000、曲はたいてい 44100。その差合わせが巻き戻りの正体かもしれない。
**曲は1曲も変換しない。メモリも時間も増えない。**
