# 現在の版

最終更新: 2026-08-10

| アプリ | 版 | sw.js |
|---|---|---|
| さいせい君 | **2026-08-10 v15 測定版** | `ipad-apps-cache-v9` |
| まるち君 | 旧方式のまま | — |

## この版について

**v13 に「測るだけ」の表示を足したもの。再生の処理は1行も変えていない。**

再生ボタンを押した瞬間に、iPadが手元に持っている音のデータ量を画面左下に出す。

- `手元のデータ=0本（からっぽ）` → iPadがデータを捨てている（読み込みが原因）
- `手元のデータ=1本 0.0〜8.3` などが出る → データはある（読み込みは原因ではない）

黄色い枠をタップすると表示が消える。

## 版の履歴

| 版 | 内容 |
|---|---|
| v15 測定版 | v13 ＋ 測定表示（現在これ） |
| v14 | 停止後の読み込み温め → 登録が固まるため取り消し |
| v13 | 停止後の頭出し修正＋版数表示 |
